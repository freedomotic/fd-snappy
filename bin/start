#!/bin/sh

appdir="$SNAP_APP_PATH"
cd "$appdir"
export JAVA_HOME="$appdir/jre"

echo Launching Freedomotic runtime...
$JAVA_HOME/bin/java -splash:${appdir}/splash.png -jar ${appdir}/freedomotic/freedomotic.jar 
	
